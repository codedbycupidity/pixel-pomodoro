// Tasks panel — placeholder for future task list UI.
// Static elements (tasks-frame, tasks-trash-button, TASKS header) come from
// App.tsx's figma-image/text loops via panel group classification.
export function TasksPanel(): React.JSX.Element | null {
  return null
}
